package au.org.ala.sensitiveData.validation;

import org.drools.runtime.rule.RuleContext;

rule "ConservationSensitive"
    when
        Category( address == null )
    then
        warning(kcontext);
end

